#include "GLBLoader.h"
#include <iostream>

MeshNode* GLBLoader::loadGLB(const std::string& path) {
    std::cout << "Model loading is temporarily disabled. Skipping: " << path << std::endl;
    return nullptr;
}
